# Optional. Enables AI-generated natural-language summaries of your meal
# plan. Leave this unset and the app still works fine - it just falls back
# to a simple templated summary instead of calling the OpenAI API.
OPENAI_API_KEY=
